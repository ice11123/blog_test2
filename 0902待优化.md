# 0902 待优化清单

> 记录日期：2026-09-02  
> 性质：临时审查与验收清单；实施状态以 Beads 为准。  
> 删除条件：以下所有条目完成修复，`pnpm test`、`pnpm run check`、`pnpm run build`、`git diff --check` 全部通过，提交推送并完成线上复核后，由最后一个实施任务删除本文件。

## P1：优先修复

- [ ] 修复管理台 ClientRouter 生命周期。
  - 现象：从管理台经站内导航离开后返回，页面停留在“等待页面初始化”，刷新按钮及管理交互失效。
  - 要求：管理台实现可重复执行的 `mount(root)` / `dispose()`，在 `astro:page-load` 重新挂载，在页面离开时释放 DOM 引用、监听器、定时器和请求。
  - 验收：连续执行“进入管理台 → 离开 → 返回”至少 10 次，状态检测、表单、目录树和按钮始终可用，监听器不重复累积。

- [ ] 修复草稿保存失败仍提示成功的问题。
  - 现象：localStorage 配额耗尽、不可用或序列化失败时，异常被吞掉，界面仍显示“已保存到本浏览器”。
  - 要求：草稿存储接口返回明确成功结果或类型化错误；写入后读回校验；只有真正成功时显示保存成功。
  - 验收：使用拒绝写入、配额不足、损坏数据等存储适配器测试，确认失败时提示明确且不会丢失当前编辑内容。

## P2：运行时可靠性

- [ ] 修复 Plot3D 主题切换后颜色不更新。
  - 现象：页面切换亮暗主题后，Plotly 画布仍保留旧主题背景和坐标颜色。
  - 验收：图表已加载时连续切换亮暗主题，画布、坐标轴和文本立即与当前主题一致。

- [ ] 修复 Plot3D 在窗口缩放和横竖屏切换后的溢出。
  - 现象：桌面宽度缩到 390px 后，图表 SVG 仍保持约 600px，页面出现横向滚动；刷新后才恢复。
  - 要求：启用可靠的响应式配置，并在容器尺寸变化时调用 Plotly resize；离开页面时释放观察器。
  - 验收：桌面、移动端、横竖屏和侧栏变化后，图表宽度始终不超过自身容器，页面无横向溢出。

- [ ] 修复 Plotly CDN 首次加载失败后的重试路径。
  - 现象：失败脚本节点被保留，后续进入文章会复用已失败脚本，只能再次等待超时。
  - 要求：外部资源加载器记录脚本状态，失败或超时后清理无效节点，并支持确定性的重新加载。
  - 验收：首次请求失败后恢复网络，再次进入文章能立即重新请求并成功绘图，无残留失败脚本。

- [ ] 修复 Mermaid 跨文章导航的条件初始化和资源释放。
  - 现象：会话首先进入不含 Mermaid 的文章时，后续站内进入含图表文章可能不渲染；反向路径会留下主题观察器。
  - 要求：始终注册路由生命周期，在每次页面加载后判断是否需要渲染，离开时释放观察器并取消过期渲染。
  - 验收：含图表与不含图表文章之间反复导航，Mermaid 始终正确渲染且没有重复监听器。

- [ ] 深化公共数据请求模块。
  - 现象：请求池只限制并发，没有统一超时、取消、重试和完整性语义；移动网络半连接时可能长期处于加载状态。
  - 要求：公共请求接口统一处理超时、取消、有限重试和错误分类；页面切换后取消无用请求。
  - 验收：断网、超时、限流、页面切换场景均在限定时间内进入明确降级状态，不无限加载。

- [ ] 阻止 GitHub Languages 缓存不完整统计。
  - 现象：部分仓库语言请求失败时仍汇总其余结果，并把不完整比例缓存 12 小时。
  - 要求：缓存数据携带完整性和版本信息；只有完整成功结果才进入长期缓存，部分结果应明确标注并短期处理。
  - 验收：模拟任一仓库请求失败，不会把部分结果伪装成完整统计长期缓存。

- [ ] 修正 Worker 部署状态错误语义和线上陈旧缓存。
  - 现象：无法读取 GitHub Actions 记录时会被转换为 `failure`；当前线上缓存仍指向 `46aeb30`，仓库实际为 `25b9323`。
  - 要求：区分 `success`、`pending`、`failure`、`unavailable`、`unknown`；只有读到明确失败结论才显示部署失败；记录缓存刷新失败原因和最近成功时间。
  - 验收：上游超时或限流时显示“状态暂不可用”而不是红色失败；上游恢复后缓存能自动更新到当前提交。

## P3：维护流程与架构

- [ ] 将本地文章发布改为可恢复的事务式流程。
  - 现象：文章文件先落盘，再执行检查、提交和推送；中途失败会留下半成品或暂存状态，阻断下次发布。
  - 要求：在临时位置完成验证后原子落盘，或精确记录并回滚目标文章的原始状态；不得影响其他用户改动。
  - 验收：分别模拟检查、构建、提交和推送失败，目标文件与暂存区均保持约定的不变量，并能安全重试。

- [ ] 用行为测试补齐现有源码契约测试的盲区。
  - 现状：部分测试只通过正则检查源代码文本，不能证明真实浏览器行为正确。
  - 要求：保留必要的静态禁令，新增管理台重复进入、草稿存储失败、CDN 失败重试、主题切换、横竖屏变化和公共请求超时的行为测试。
  - 验收：上述缺陷能够先由测试稳定复现，再由修复使测试通过。

- [ ] 清理 Astro Markdown 配置弃用警告。
  - 现象：`check`、`build` 和 `preview` 持续输出 `markdown.remarkPlugins`、`markdown.rehypePlugins`、`markdown.remarkRehype` 弃用提示。
  - 验收：保持 Markdown、KaTeX、标题锚点和 Mermaid 输出不变，构建过程不再出现该弃用警告。

- [ ] 控制首页维护记录造成的持续增长。
  - 现状：首页约 68.7KB HTML、742 个初始 DOM 节点，维护记录继续追加后会不断增加下载、解析和 DOM 创建成本；`content-visibility` 不能消除这些成本。
  - 要求：主页只展示最近若干条维护记录，完整历史迁移到独立归档页或按需加载，同时保留可访问性和直达链接。
  - 验收：首页初始 DOM 保持明显低于 800，新增维护记录不会导致首页体积无限增长。

- [ ] 完善搜索框的组合框无障碍语义。
  - 现状：结果列表使用 `listbox/option`，但输入框缺少 `combobox`、`aria-controls` 和 `aria-activedescendant`，键盘选中结果时读屏器难以获知当前项。
  - 验收：键盘上下选择、Enter 打开、Escape 关闭均可用，并能由读屏器正确播报当前结果和总状态。

- [ ] 为关键架构约束补充正式决策记录。
  - 范围：客户端路由运行时、公共状态语义、云发布关闭但只读 Worker 已接入、外部脚本加载策略和草稿持久化不变量。
  - 要求：不要把稳定约束继续散落在维护公告和实现细节中；使用仓库既有文档体系记录术语、接口和决策原因。

## 本轮明确不包含

- 外部攻击面、CSP、第三方输入攻击模型和依赖漏洞升级，继续按既有决定延期。
- 删除三层波浪、主题圆形切换、壁纸抽屉、磨砂或其他现有效果来换取性能。
- 未经明确授权部署 Worker 或启用云发布。

## 最终清理条件

最后一个实施任务必须同时确认：

1. 本文件所有复选框均已完成并有对应测试或浏览器证据。
2. 根项目与 Worker 测试、Astro 检查、生产构建、差异检查全部通过。
3. 改动已提交并推送到 `origin/main`，GitHub Pages 部署成功。
4. 线上桌面与移动端完成复核，没有新增回归。
5. Beads 中对应任务全部关闭并记录提交哈希。

满足以上条件后删除 `0902待优化.md`，删除动作与最后一批修复一起提交；任何条目未完成时不得提前删除。
