# blog_test2 视觉设计规范

## 1. 设计目标

`blog_test2` 是“离子怪的博客”的独立视觉实验版。首页优先表达作者、文章和持续创作，其次才展示代码统计、运行状态与维护记录。设计通过排版、留白和内容密度建立层级，避免把每个模块都包装成同等重量的仪表盘卡片。

本轮只约束首页与首页直接使用的共享状态。其他页面继续沿用现有样式，待首页确认后再逐步迁移。

## 2. 设计原则

1. **内容优先**：近期文章是首页第一交互目标。
2. **层级少而清晰**：页面只保留主内容、次级信息和浮层三种表面层级。
3. **状态色只表达状态**：品牌色不代替成功、警告或错误色。
4. **静态信息不伪装成交互**：不可点击的统计项不使用上浮、缩放或指针光标。
5. **中文排版优先**：标题允许自然换行，不为追求整齐而截断关键信息。
6. **移动端重新编排**：不是简单缩小桌面布局，而是调整优先级和信息顺序。

## 3. 颜色 Token

首页组件只使用以下语义 Token。`light` / `dark` 两套主题只替换具体颜色值，不改变 Token 的职责。

| Token | 角色 |
| --- | --- |
| `--canvas` | 页面底色 |
| `--surface` | 主要内容表面 |
| `--surface-subtle` | 次级信息表面 |
| `--text-primary` | 标题、正文和关键数值 |
| `--text-secondary` | 描述、时间和辅助信息 |
| `--accent` | 链接、当前导航和主要交互 |
| `--border` | 必要的结构边界 |
| `--focus` | 键盘焦点轮廓 |
| `--success` | 成功与正常状态 |
| `--warning` | 检测中、等待和未配置状态 |
| `--danger` | 已确认的错误状态 |

要求：

- 页面背景与主表面的明度差必须比主表面与次级表面的差更明显。
- 品牌色不用于大面积背景。
- 普通结构边框使用低对比度 `--border`，状态卡边框使用对应状态色。
- 正文与背景的对比度目标不低于 WCAG AA。

## 4. 字号层级

| 层级 | 字号 | 用途 |
| --- | --- | --- |
| Meta | `12px` | 日期、标签、状态细节 |
| Secondary | `14px` | 辅助说明、侧栏、按钮 |
| Body | `16px` | 正文、摘要、导航 |
| Section | `20px` | 首页模块标题 |
| Page | `28px` | 普通页面标题 |
| Hero | `48px` | 首页主标题；移动端 `32px` |

- 正文行高为 `1.65`，标题行高为 `1.25～1.35`。
- 首页不新增体系外字号。
- 英文眉题不是默认标题组成，仅在确有必要的技术信息区使用一次。

## 5. 4/8px 间距体系

允许值：`4 / 8 / 12 / 16 / 24 / 32 / 40 / 48px`。

- 图标与文字：`8px`
- 控件内部：`8px 12px`
- 卡片内部：`16px`，内容较多时 `24px`
- 同组模块：`16px` 或 `24px`
- 不同章节：`32px` 或 `48px`
- 页面左右安全边距：桌面 `24px`，移动端 `16px`

## 6. 圆角和阴影

| Token | 值 | 用途 |
| --- | --- | --- |
| `--radius-control` | `8px` | 按钮、输入框、状态项 |
| `--radius-surface` | `12px` | 内容面板、文章卡 |
| `--radius-media` | `16px` | 主视觉或大型媒体容器 |
| `--radius-pill` | `999px` | 标签、状态徽标 |

阴影仅允许用于：

- 弹窗和下拉菜单；
- 移动端抽屉；
- 明确可点击的媒体文章卡 Hover 状态。

普通面板、统计项、维护公告和系统状态不使用阴影。

## 7. 组件状态

### 加载

- 使用与最终内容尺寸接近的骨架，避免布局跳动。
- 骨架动画周期不短于 `1200ms`，并遵守 `prefers-reduced-motion`。

### 空状态

- 说明“为什么为空”，不要只写“暂无数据”。
- 存在可执行下一步时提供一个低权重链接。

### 错误状态

- 使用 `--danger`，展示可读原因和重试按钮。
- 单个模块错误不得阻断其他模块显示。

### 禁用状态

- 禁用按钮必须有 `disabled` 属性，并在附近说明原因。
- 禁用状态不得仅依赖降低透明度表达。

### 长文本

- 页面标题、文章标题和状态标题允许换行。
- 首页文章摘要最多显示两行。
- 路径、URL 和长英文单词使用 `overflow-wrap: anywhere`。
- 只允许在摘要、非关键标签和纯装饰信息中使用省略号。

## 8. 动效与交互

- 高频按钮反馈为 `150～200ms ease-out`。
- 页面首屏核心内容不等待滚动观察器，不使用长时间淡入。
- Hover 动效仅在 `hover: hover` 且 `pointer: fine` 的设备启用。
- 静态统计项不位移；可点击文章卡最多上移 `2px`。
- 所有按钮和链接必须具有清晰的 `:focus-visible` 状态。

## 9. 主题实际值

### Light

- `--canvas: #fdfbfb`
- `--surface: #ffffff`
- `--text-primary: #2f2d30`
- `--text-secondary: #686269`
- `--accent: #e9536a`
- `--border: #e8dde0`

### Dark

- `--canvas: #000123`
- `--surface: #000014`
- `--text-primary: #eaedf3`
- `--text-secondary: #c9cfdd`
- `--accent: #617bff`
- `--border: #303f8a`

旧主题值统一迁移：`dark-* -> dark`，`light-* -> light`。主题菜单不得继续暴露旧六套选项。

## 10. 首页信息顺序

### 桌面端

1. 站点主标题与作者简介；
2. 近期文章；
3. 紧凑统计摘要；
4. 语言构成；
5. 系统状态与维护公告；
6. GitHub 贡献记录。

### 移动端

1. 站点主标题与简介；
2. 近期文章；
3. 紧凑统计摘要；
4. 维护公告；
5. 语言构成；
6. 系统状态；
7. GitHub 贡献记录。

## 11. 验收视口

- 桌面：`1440×900`
- 移动：`390×844`

必须检查：文字截断、页面横向溢出、按钮层级、键盘焦点、颜色对比度、加载/空/错误/禁用状态和长文本换行。

## 12. 首页封面与作者卡

- 首页封面使用主题自适应渐变、轻量网格与两层 SVG 波纹，不使用单个大字球或无业务含义的轨道装饰。
- 波纹仅承担封面到内容区的层级过渡；动画周期保持 7～11 秒，并在 `prefers-reduced-motion` 下停用。
- 作者信息卡固定包含头像、简介、社交入口、文章/分类/标签统计和快捷导航；普通内容卡不复制其悬浮阴影。
- 头像使用 `object-fit: cover` 的圆形裁切，边框颜色来自 `--surface`，不得硬编码与主题冲突的白边。
- 封面文案容器宽度必须受视口约束；英文眉题允许在窄屏换行，禁止依靠页面裁切隐藏内容。
