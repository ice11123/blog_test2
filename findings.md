# blog_test2 设计与工程发现

> 历史归档：此文件保留既往设计证据，不再作为当前任务或缺陷状态来源；当前状态以 Beads（`bd`）为准。

## 2026-08-15｜V2.3 顶部图片主视觉审计

- 用户提供图片尺寸为 `2837×1600`、约 326KB，横向构图，人物面部位于画面中右侧，适合使用 `object-fit: cover` 作为封面背景。
- 桌面端默认裁切焦点应落在人物面部附近；移动端因容器更窄，需要单独调整 `object-position`，不能沿用桌面值后仅依靠缩放。
- 现有封面同时包含渐变背景、网格、椭圆 ribbon、光晕和波浪。加入真实图片后应移除旧 pattern/ribbon/glow，避免视觉元素互相竞争；底部波浪可继续作为封面到正文的过渡。
- 滚动缩放只应更新图片元素的 `transform`，使用 `requestAnimationFrame` 合帧；不动画宽高、内边距或父级 CSS 变量，避免滚动过程中产生布局抖动。
- 用户所说“往上滑”按触控习惯解释为手指上滑、页面 `scrollY` 增加；返回页面顶部时图片应平滑恢复默认裁切。
- 当前可用技能中没有名为 `mail` 的 skill；本轮使用最接近的 `emil-design-eng`，重点遵守仅动画 transform/opacity、可中断、减少动态兼容和实际截图复核。
- 首次浏览器实测在 800×600 下：封面高 470px、图片初始缩放为 `1.025`、裁切焦点为 `52% 43%`，标题位于左侧暗部，人物面部仍可见，页面级 `scrollWidth` 未超过视口。
- 初始截图确认旧 ribbon/glow/pattern 已消失，图片、标题和双层波浪的视觉层级清楚；浅色红金波浪与粉紫图片之间存在自然过渡，不需要再叠加主题滤镜。
- 1440×900 实测首页主体宽 1156.4px、左侧栏常驻，页面 `scrollWidth=1430 < 1440`；封面默认 `scale(1.025)`，滚动至 300px 后为 `scale(1.1359)`，动效幅度可感知且未触发布局变化。
- 390×844 实测封面高 330px、移动裁切焦点 `54% 43%`，标题边界完整，页面 `scrollWidth=390`；滚动至 220px 后缩放为 `1.122`，人物面部和标题均没有横向裁切。
- 移动截图中现有左栏抽屉按钮会覆盖封面与正文交界左侧约 36px，这是既有导航交互，不是图片溢出；当前不扩大本轮范围修改侧栏业务。
- 深色 390×844 截图确认黑色遮罩、金色波浪和白色标题保持可读，图片没有被主题滤镜染脏。
- 文章页 1440×900 回归确认共享左栏显示新座右铭，文章导航和当前文章选中态仍存在，首页专用图片脚本没有向文章页注入图片节点。

## 2026-08-15｜基线审计

- `blog_test1` 当前 `main` 为 `c77071d`，工作区干净；`blog_test2` 从该提交保留完整历史创建。
- 当前首页约一半可见元素带结构边框或圆角，重复的“半透明面板 + 细边框 + 大圆角”使模块权重接近，形成模板化仪表盘观感。
- 当前字号存在大量 9～19px 的零散计算值；新首页必须收敛到 12/14/16/20/28/40px 体系。
- 首页的主要业务内容是作者简介、近期文章和文章数据；系统状态、维护公告、语言统计和贡献墙应降为次级信息。
- 移动端当前无页面级横向溢出，但顶栏和六张统计卡占据较多首屏空间。
- `blog.symb0x76.top` 的可取之处是依靠主视觉、留白、内容节奏和单一强调色建立层级，而不是为每个信息块增加边框。
- `blog.rouming-fei.top` 当前访问超时，本轮不使用无法验证的视觉细节。

## 2026-08-15｜仓库隔离

- `blog_test2` 需要同时替换 Astro base、站点 URL、robots、README、公共仓库状态和浏览器存储命名空间。
- 仅替换 Astro base 不足以隔离管理台：旧代码的 Worker、OAuth 回跳、公开状态和发布目标仍指向 `blog_test1`。
- 本轮采用显式发布开关，管理台发布按钮禁用且不发起 OAuth、Worker 或 GitHub 写请求；本地草稿继续可用。
- 新仓库不部署 Worker；Pages 工作流只执行前端检查和构建。

## 2026-08-15｜首页实现发现

- 首页原有 `BlogList` 和语言统计组件可继续复用，但必须在首页作用域取消嵌套卡片边框，否则会与新外层信息结构重复。
- 当前内容更适合“两列首屏 + 两列次级信息 + 底部贡献墙”：作者与文章为第一层，语言和运行信息为第二层。
- 系统状态仍保留真实的加载、等待、正常与错误语义；实验版没有 Worker 时应显示黄色“未接入”，而不是红色故障。
- Astro `base` 已统一为 `/blog_test2`，站内链接继续通过 `withBase()` 生成，不需要逐页硬编码子路径。

## 2026-08-15｜视觉验收结论

- 1440×900 下左右侧栏展开后，中央首页实际宽度约 870px；首版把主视觉内部再分列会把 450px 左栏压成逐字换行，因此主视觉改为单列，统计网格放在正文下方。
- 首页原有文章布局标题区会重复显示日期和站名，移动端尤其浪费首屏；新增 `hidePageHeader` 参数，只在首页隐藏该区，文章页保持不变。
- 语言面板与运行信息处在同一网格行时不能使用默认拉伸，否则公告较高会在语言面板内部制造大块空白；首页网格改为 `align-items: start`。
- 390×844 下核心标题、摘要、按钮、统计和近期文章边界均位于视口内；贡献墙仅在自身滚动容器中横向扩展，页面级没有横向滚动。
- 管理台审计结果：同步端点为空、`data-publish-enabled="false"`、发布按钮禁用，提示明确说明不会请求 OAuth、Worker 或 GitHub 写接口。
- 搜索弹窗可打开；六套主题均能切换到对应 `data-theme`；桌面和移动截图已保存到 `artifacts/`。
- Pages 首次成功部署仍可能发布错误子路径：本轮 `astro.config.mjs` 未进入首个提交，线上 HTML 同时出现新首页结构和旧 `/blog_test1/` 资源地址。线上 URL、资源路径和文章链接必须作为独立验收项，不能只看 Actions 绿色状态。

## 2026-08-15｜线上子路径复核

- 修复提交 `f3409f8` 部署后，线上首页返回 200，首个样式资源为 `/blog_test2/_astro/global.CRaXZZ9p.css`，favicon、RSS、Sitemap 和 canonical 也全部指向 `/blog_test2/`。
- `robots.txt` 的 Sitemap 为 `https://ice11123.github.io/blog_test2/sitemap-index.xml`；Sitemap 中存在 `/blog_test2/`，不存在 `/blog_test1/`。
- 首页 HTML 仍包含字符串 `blog_test1`，来源是维护公告中的历史文字，不能再用全局字符串存在性判断子路径是否正确；应只检查 `href`、`src`、canonical、RSS 和 Sitemap 等 URL 字段。
- GitHub Actions 公共 API 显示运行 `31878284223` 已成功发布提交 `f3409f81890679a0cae10e90700bdc38593fdba5`。

## 2026-08-15｜线上浏览器验收

- 线上首页在 `1440×900` 与 `390×844` 视口均可加载；CSS、canonical、RSS 和首篇文章链接均指向 `blog_test2`。
- 桌面视口页面级无横向溢出；移动视口 `document.documentElement.scrollWidth` 小于可视宽度，侧栏越界仅存在于隐藏抽屉区域，不影响页面滚动。
- 精确尺寸截图已保存为 `artifacts/blog-test2-online-1440x900.png` 与 `artifacts/blog-test2-online-390x844.png`。第一次未使用 clip 时截图被浏览器可见区域裁成 `1435×807` 与 `386×241`，已改用显式 clip 重取 1440×900 与 390×844，避免误判布局。
- 搜索按钮点击后搜索对话框实际变为 `display: block` 且可见；主题按钮可交互，当前主题状态正常。
- `/admin/` 线上页面的 `data-publish-enabled` 为 `false`，发布按钮禁用，无 OAuth 链接、Worker URL 或同步端点。
- 浏览器日志出现 GitHub 贡献数据请求失败警告；该模块按设计显示错误/空状态，不阻断首页。历史旧 URL 日志来自同一浏览器会话的先前页面，当前线上 DOM 未发现 `/blog_test1/` 资源属性。

## 2026-08-15｜V2 参考站与现有骨架审计

- 当前首页在 900px 视口仍保留左右各约 160px 侧栏，中央主视觉和文章区域只有约 453px；这不是 Token 问题，而是首页错误复用文章三栏布局。
- 当前系统状态区域在该视口高约 901px，语言区域约 513px，近期文章、语言、状态、贡献全部使用同等 12px 圆角面板，形成模板化仪表盘观感。
- `blog.symb0x76.top` 使用无首页侧栏的全宽内容舞台、48px 主标题、16px 正文、20px 文章标题和横向媒体文章卡；页面层级主要依靠主视觉、留白、内容节奏和少量阴影建立。
- V2 必须拆分公共无侧栏布局与文章三栏布局；仅在现有首页 CSS 上继续调整无法解决正文宽度和信息优先级问题。
- 用户确认：首页与公共顶栏重做；文章页保留侧栏；运维模块降到页尾紧凑信息带；主视觉使用 CSS/文字；主题精简为浅色/深色两套。

## 2026-08-15｜V2 实际浏览器复核

- 新增 `PublicLayout` 后仍需显式覆盖全局 `main { width: 720px }`；否则首页 DOM 虽已换骨架，视觉仍会被旧正文宽度限制。
- 首轮截图发现 `hero-art` 在 DOM 中先于 `hero-copy`，却未指定网格位置，导致装饰占左列、正文被压到右侧并裁切；通过固定正文为第一列、装饰为第二列修复。
- 移动端通用规则隐藏了 `.header-tool-button > span`，连搜索图标外层 `.icon` 一起隐藏，造成空白按钮；改为只隐藏非图标文本并固定 36px 控件宽度。
- 主题菜单选择后原脚本只切换主题、不收起菜单；补充 `hideDropdown()`，避免移动首屏被下拉层遮挡。
- 1440×900 首页实测无左右侧栏，主舞台宽 1200px；390×844 页面级宽度未超过视口。
- 文章页 1440px 实测继续保留左右各 240px 侧栏，正文约 858px；移动端保留两侧栏切换按钮，三栏业务结构未回归。
- 浏览器中浅色/深色选项均可见，搜索弹窗可打开，旧顶栏折叠按钮已移除。
- Pages 线上部署提交 `8f810db` 后，桌面主舞台仍为 1200px、首页侧栏数为 0；移动端页面宽度未超过 390px，说明本地与线上构建结果一致。

## 2026-08-15｜V2.1 头像与波纹视觉复核

- 用户提供的头像为猫咪与拳头图片，已复制到 `public/avatar.jpg`，作者卡使用圆形裁切并保留主题边框。
- 桌面截图确认顶部封面、两层动态波纹、作者信息框均已出现，原“离”字大球及轨道装饰已移除。
- 移动截图发现英文眉题因封面文案容器按内容宽度收缩，右侧出现裁切；根因不是页面横向溢出，而是局部文案缺少可收缩宽度。
- 已将封面文案容器改为受视口约束的 `width: min(...)`，移动端眉题允许换行并降低字距；下一轮截图需确认完整显示。
- 修正后 1440×900 实测封面眉题范围为 `354～1086px`，头像图片加载成功，页面 `scrollWidth` 为 1430，不超过视口。
- 修正后 390×844 实测眉题范围为 `40.8～349.6px`，作者卡范围为 `20.8～369.6px`，页面 `scrollWidth` 为 381，不超过视口。
- 深色主题下波纹前景正确使用主题画布色，头像路径为 `/blog_test2/avatar.jpg`，移动端仍无页面级横向溢出。
- GitHub Pages 运行 `31883421811` 成功部署提交 `565382d1116be077ff6d6416cc667a61e39ad60a`。
- 线上 1440×900 确认封面高度 470px、波纹节点存在、头像加载成功、旧 `.hero-mark` 不存在，页面宽度未超过视口。
- 线上 390×844 确认眉题不裁切，页面 `scrollWidth` 为 381，头像仍从 `/blog_test2/avatar.jpg` 加载。

## 2026-08-15｜V2.2 波浪与左栏复现结论

- 浏览器确认 `prefers-reduced-motion: reduce` 为 `false`，波浪动画名称、时长和运行状态均正常，不是动画被禁用。
- 前层波浪 0.9 秒内 transform 只从 `translateX(15.5px)` 变化到 `11.2px`；在 1440 宽 SVG 上肉眼近似静止，根因是运动幅度相对画布过小。
- 当前文章页能生成 `.left-sidebar a.current`，链接与 URL 正确；但桌面左栏仍受 `left-sidebar-collapsed` 和本地状态控制，无法保证每次导航后常驻。
- 修复方向：波浪使用超宽重复路径与线性水平循环，只动画 transform；桌面移除左栏折叠状态和按钮，移动端继续使用覆盖抽屉。
- 新增浏览器诊断发现：文章页通过 `BlogCard.astro` 的 `@use '../../styles/global.scss'` 重复注入旧主题 Token，导致首页主题已更新而文章页仍显示蓝色；已移除该全局 Sass 复用并将卡片下划线样式改为局部 CSS。
- 桌面文章页实测左栏宽 220～240px、导航“文章”和当前文章链接均带 `aria-current="page"`；移动端左栏隐藏为抽屉，页面宽度仍不超过 390px。
- 移动端文章标题字号由全局 39.6px、1.8 行高收敛为 `clamp(2rem, 9vw, 2.35rem)` / 1.35，避免长中文标题靠近裁切边界。
- 复核新需求时发现首页的“左栏”其实是 `index.astro` 内部的独立作者卡，而文章页使用 `SidebarLeft`，两者不是同一组件，所以从首页进入文章后无法形成视觉连续性。
- 修复策略调整为：首页 `PublicLayout` 直接复用 `SidebarLeft` 和 `sidebar-tree` 交互；删除首页内部重复作者卡。桌面端同一左栏常驻，移动端仍使用同一覆盖抽屉。
- 1440px 实测新波浪前层 0.9 秒位移约 75px（约 `-8px` 到 `-83px`），原“动画运行但肉眼近似静止”的根因已消除。
- 首次将共享左栏接入首页后的 1440×900 截图暴露新布局问题：封面主体被推到首屏下半部，左栏上方与封面内容缺乏同一基线，视觉上留下大块空区。不能以“左栏已出现”作为验收通过，需继续检查 flex 子项的基线/垂直定位并修复。
- 进一步检查发现这不是 CSS 设计本身，而是浏览器仍连接到旧 dev server：页面计算样式仍包含旧的 `.public-main { width:100% }`，新 `.public-layout-frame { display:flex }` 未进入样式表。需重启本地服务后再做布局结论，避免把热更新失效误判为代码根因。
- 重启到 `4323` 后计算样式已生效：首页左栏宽 240px、封面从 x≈279px 开始，波浪前层仍持续位移；桌面截图确认首页与文章页共享左栏。
- 当前 In-app Browser 的 viewport `set({390,844})` 在现有会话中未改变 `innerWidth`（仍为 1440/新标签 1280），所以移动端需改用可用的浏览器视口能力或本地 Playwright 作为补充，不能把裁剪截图冒充响应式验收。
- 黑金桌面截图视觉对齐正常；首次自动切回浅色时选项点击未生效且菜单保持展开，生成的所谓“浅色截图”实际仍是深色，必须检查 `data-theme` 后再保存，不能仅按操作意图命名产物。
- 复看既有 390×844 文章截图时发现移动端文章标题仍有右侧裁切迹象；抽屉打开时左栏选中态正常，但标题换行需再检查当前 CSS 是否被旧 `.title h1` 规则或布局宽度覆盖。
- 构建产物核对脚本首次使用了 PowerShell 保留变量 `$home`，触发只读变量错误；改用任务专用变量名重跑，页面构建本身不受影响。
- 推送后查询 GitHub Actions 公共 API 被匿名 API rate limit 拒绝；改用 Pages URL 响应、GitHub Actions 网页和提交内容间接复核，不把 API 限流误判成构建失败。
- Actions 网页显示运行 `31885748285` 已成功，部署提交为 `e1c0787`；线上首页与文章页 1440px 均能找到 240px 左栏，首页波浪为 `cover-wave-scroll` 18s，文章页主导航和当前文章均有 `aria-current=page`，资源属性中没有 `/blog_test1/`。
