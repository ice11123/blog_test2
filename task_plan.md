# blog_test2 V2 内容优先视觉重构

## 目标

基于 `blog.symb0x76.top` 的可验证布局语言，重做 `blog_test2` 首页与公共顶栏：首页移除左右侧栏，改为内容优先主舞台；运维信息降级为页尾紧凑信息带；六套主题迁移为浅色/深色两套；文章页侧栏和现有业务逻辑保持不变。

## Beads

- 当前任务：`blog_test2-car`
- 状态：进行中

## 阶段

- [x] 审计参考站与现有首页，确认根因是旧三栏骨架和同权重面板。
- [x] 新增无侧栏公共布局，首页脱离 `BlogPost` 三栏结构。
- [x] 重做公共顶栏并保留搜索、社交、隐藏管理入口。
- [x] 重做首页主视觉、近期文章和页尾紧凑信息带。
- [x] 将六套主题迁移为 `light` / `dark`，兼容旧存储值。
- [x] 更新设计规范、维护公告、发现与进度记录。
- [x] 通过自动检查与本地桌面/移动浏览器复核。
- [ ] 推送 `main`、完成 Pages 部署和线上复核。

## 约束

- 只修改 `blog_test2`，`blog_test1` 保持只读。
- 首页无侧栏；文章页继续保留左右目录侧栏。
- 不复制参考站文章、图片或个人资料。
- 不启用 Worker、OAuth 或云端发布。
- 保留 `/blog_test2/` 子路径、搜索、RSS、Sitemap、Markdown/MDX 和管理台禁写状态。

## 错误记录

- `blog.rouming-fei.top` 在 PowerShell 与浏览器中持续超时，本轮只使用可验证的 `blog.symb0x76.top` 作为主参考。
- 参考站页面截图在当前浏览器控制接口中因页面渲染负载连续超时；改用 DOM、计算样式和布局尺寸完成循证审计，不伪造不可获取的截图结论。

- GitHub 网页创建仓库时可用性检查超时；改用本机 Git Credential Manager 凭据调用 GitHub 官方 API，凭据未打印或落盘。
- 当前终端未安装 `gh`，GitHub Pages 专用 REST 端点匿名访问返回 404；改用无需认证的 Actions 公共 API核对实际发布提交和结果。
- 浏览器截图首次等待 `networkidle` 不受当前控制接口支持；改用 `load` 状态并显式 `clip` 生成准确视口截图。
- 首次 Beads 初始化继承了 `blog_test1` 的 Dolt 元数据并拒绝覆盖；更新新仓库配置后使用受保护的本地重初始化流程，建立独立 `blog_test2` 数据库。
- 首页首次重构补丁残留了一组旧的近期文章覆盖样式；检查生成文件后已删除，避免旧规则反向覆盖新视觉。
- 管理台浏览器审计首次使用 `instanceof HTMLButtonElement` 时，隔离执行上下文没有暴露该构造器；改为检查 `disabled` 属性完成同等只读验证。
- 截图工具保留了上一次滚动位置，并且默认移动截图高度不足；改用页面级滚动归零和显式 `clip`，最终截图尺寸为 1440×900 与 390×844。
- 六主题首次循环测试在下拉菜单已展开时再次点击按钮，导致菜单被关闭；后续先读取 `hidden` 状态，仅在需要时打开，六套主题全部成功应用。
- 首次提交命令的显式文件列表漏掉 `astro.config.mjs`，导致线上 HTML 已是新首页但静态资源和站内链接仍使用 `/blog_test1/`；线上复核发现后补交 base 配置并重新部署。
- 生产产物主题字符串检查首次在 PowerShell 双引号中使用正则竖线，表达式被 shell 误解析为命令；改用单引号保护正则后继续检查。
