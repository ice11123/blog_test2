# 博客维护公告

## 2026-09-20｜电控专题教学化重写

- PID、非抢占式调度器、灰度循迹、卡尔曼滤波与姿态传感器共 13 篇文章改为面向新生的教学结构，从工程问题出发解释原理、代码路径、验证方法和故障排查。
- 删除解压、压缩包、资料整理和内部审查状态等与学习无关的表述；标题与导语改为能力导向，文末使用工程接入检查表和“本篇总结”。
- 新增教学文档回归测试，防止后续文章重新出现资料交接式口吻，并继续保留代码调用状态、硬件安全边界和待实测参数。

## 2026-09-19｜主页技术星图与四大分组入口

- 主页新增 AI/Agent协作与开发、电控、电源、其他四个技术分组，采用不对称知识星图卡片与独立主题图形，和实验室站点的横向展台视觉保持区分。
- 电控卡使用用户提供的透明小车素材，AI/Agent 卡使用本站原创 Codex 风格代码轨道标识；图片生成响应式 AVIF/WebP，不增加外部首屏请求。
- 整张分组卡均可进入分类，卡内近期文章仍直达正文且拥有独立反馈；空分类保留真实的零文章状态，并补齐键盘焦点、按压、降低动态和移动端布局。

## 2026-09-19｜分层文章目录与分类浏览优化

- 文章总目录改为一级分类总览，每个分类仅展示最近更新的 3 篇文章，标题与摘要可直接进入正文，不再在总览页一次性铺开全部二级目录。
- 一级分类标题与“查看完整分类”入口均可进入分类页；分类页继续展示该分类下的二级目录和完整文章列表，浏览层级更明确。
- 主页近期文章继续保持整卡直达正文，并补充回归约束；沿用本站原有分类图案、主题色和降低动态规则，没有引入 GUET_428 的图片或视觉配色。

## 2026-09-18｜文章目录层级与分类识别重构

- 目录页移除与文章内容无关的构建日期，改为紧凑页头、真实文章数量和明确的标签入口。
- 一级分类改为统一分组面板，二级分类与文章数量形成清晰层级；文章改用紧凑纵向列表，标题、摘要、日期和标签在桌面与移动端均保持易读。
- 目录专用列表与通用时间流卡片解耦，分类页、标签页及现有主题样式不受影响；补充精确指针悬停、按压反馈和降低动态降级。
- 一级分类增加稳定的身份图案、序号与分类配色，二级分类使用分支标记和独立轨道；现有文章同步补齐真实二级分类，目录层级不再只依赖字号猜测。

## 2026-09-18｜文章阅读界面重构

- 文章页改为紧凑导语、分层元信息与独立正文版心，长标题在桌面和移动端均能更快进入正文。
- 保留左侧个人/目录/标签目录与右侧文章目录，并降低移动端侧栏按钮的视觉干扰；亮暗主题与可选封面继续共用同一阅读结构。
- 文章作者名改为醒目的红色链接，可直接返回离子怪博客主页。

## 2026-09-16｜壁纸抽屉合成路径优化

- 将壁纸从逐帧 `clip-path` 裁切改为外层舞台与内层视口的互逆 `transform`，保持封面到全图的连续取景，同时避免每帧重绘大幅图片。
- 手势时间线在稳定态复用，高清原图延后到展开稳定后加载和解码；快速反向操作从当前进度继续，不再重复创建整组动画或与首次展开争抢主线程。
- 三层波浪、主题圆形扩散、磨砂、左侧栏与抽屉视觉全部保留；桌面 CPU×4 复测无长任务，未部署 Worker。

## 2026-09-16｜移动端顶部导航视觉精修

- 移动端首行收敛为“站名 + 搜索 / 主题 / 更多”，GitHub、Bilibili 与 QQ 统一收入更多菜单，减少图标堆叠。
- 修复更多菜单错误继承正文折叠卡片边距和内边距的问题；第二行改为等宽分段导航，当前页面通过轻量底色而非长下划线标识。
- 320px 与 390px 顶栏统一压缩到约 99px，保留足够触控面积，并继续向左右侧栏和主页壁纸提供实时顶栏高度。

## 2026-09-16｜移动端双侧栏与窄屏布局修复

- 移动端左侧新增小箭头，可展开与桌面共用的“个人 / 目录 / 标签”侧栏；文章页右侧新增独立箭头，可展开当前文章目录，不再因小屏样式直接隐藏两侧导航。
- 两侧抽屉使用实测顶栏高度计算可用空间，修复 320px 窄屏双行顶栏下抽屉错位；支持遮罩关闭、Esc、焦点恢复、互斥展开和页面切换清理。
- 文章目录项改为原生按钮，补齐键盘焦点；390×844、320×568 和 1440×900 实机浏览器视口均确认无横向溢出，桌面布局保持不变。

## 2026-09-16｜首页波浪动画性能优化

- 调整三层波浪的动画承载结构，减少持续播放时反复布局和绘制的开销，让滚动与壁纸交互有更多运行余量。
- 保留原有波形、配色、错位和播放速度，以及离屏暂停、降低动态和跟随壁纸抽屉移动的行为。

## 2026-09-15｜滚动与壁纸交互性能优化

- 阅读首页正文时释放阻塞式滚轮监听，普通浏览提前跳过壁纸的布局检查；回到顶部后恢复全局展开手势。
- 高频滚轮与拖动输入合并到每个显示帧，保留最新进度与反向操作，减少重复动画更新。
- 触摸监听按会话挂载，页面切换前释放手势、观察器和待执行动画帧；壁纸、抽屉、三层波浪和主题视觉效果均保留。

## 2026-09-15｜公共页面与首页信息密度精修

- 320px 窄屏顶栏保留完整站名，并将社交入口收纳为可键盘操作的更多菜单；桌面顶栏结构与交互保持不变。
- 关于和友链不再显示容易被误解为文章发布时间的建站日期；单个友链卡限制阅读宽度，并把 GUET_428 图标改为本站静态资源，弱网下不再依赖跨站图片请求。
- 首页文章卡用轻量序号替代重复文档图标，运行状态卡收紧高度，维护公告首页只保留最近 3 条；完整状态详情、刷新能力与维护归档均保留。
- 近期文章拆为独立组件，降低后续卡片迭代误触壁纸、三层波浪和手势代码的风险；本轮没有修改或削减这些视觉动效，也未部署 Worker。

## 2026-09-14｜首页阅读布局与更新列表优化

- 近期文章改为紧凑卡片，最多展示 4 篇；宽屏下 3 篇文章并排展示、4 篇采用双列，小屏自动切换为单列。缩小文档缩略图，精简摘要和标签，保留完整标题与整卡阅读入口。
- 维护公告改为独立时间线列表，日期、标题、摘要与最新标记分层呈现；点击单条即可展开详情，完整记录仍可进入归档查看。
- 统一代码语言、运行状态和贡献记录的面板圆角、留白与标题层级，保留原有刷新入口和按需加载；主页壁纸、三层波浪、侧栏及主题切换效果保持不变。

## 2026-09-12｜同步同源项目可靠性修复

- 对照 GUET_428 中已验证的修复逐项复现并移植：管理台切换文章、新建或离开页面前会保护尚未保存的正文；删除最后一篇草稿后会清空表单；导出文件名、格式与正文统一采用当前表单快照。
- 草稿存储遇到部分损坏数据时停止写入并保留原始内容，旧版损坏数据不再被错误迁移为空；管理预览只剥离文档开头的 frontmatter 与 MDX 导入，不再吞掉正文代码或分隔线。
- 发布脚本兼容 Windows 的 `pnpm.cmd` 调用；Plot3D 采样包含区间两端；首页壁纸手势不再抢占弹窗、输入控件、图表和独立滚动区。
- 公共状态请求会正确重试正文超时，GitHub 直连成功显示为正常；取消、跳过或未知结论不再被误报为部署失败。此次未修改任何样式、布局或动画效果，也未部署 Worker。

## 2026-09-02｜全站目录与文章目录分栏

- 桌面端常驻左栏统一为“个人、目录、标签目录”三个可自由切换的面板；文章目录按现有一级、二级分类生成，标签目录显示文章数量并高亮当前标签。
- 文章详情的标题目录从左栏拆出，固定在正文右侧并继续提供滚动定位；全站信息与当前文章信息不再混用同一个切换槽。
- 保留小屏正文优先策略、键盘方向键切换和降低动态支持；本轮未修改壁纸、波浪、主题切换或 Worker。

## 2026-09-02｜运行时可靠性与维护流程闭环

- 管理台改为可重复挂载和释放的页面生命周期，返回管理页后状态检测、刷新和编辑交互不再失效；草稿存储写入、迁移与清空均增加读回校验和明确错误反馈。
- Plot3D 支持主题即时同步与容器响应式缩放，Plotly 和 Mermaid 的外部脚本加载失败后会清理无效节点并允许后续页面重试；跨路由观察器与图形资源会在离开页面时释放。
- 公共数据请求统一使用并发池、总超时、调用方取消、一次瞬时失败重试和错误分类；GitHub Languages 仅在全部仓库统计成功时写入长期缓存，部分结果会明确标记。
- 发布脚本在检查、构建或提交失败时精确回滚目标文章，推送失败则保留本地提交并支持 `--resume-push` 安全续传，不影响工作区中的其他改动。
- 首页只展示最近 5 条维护公告并新增完整归档页；搜索框补全组合框、活动结果与展开状态语义；新增架构决策记录固定客户端路由、公共状态、草稿和外部脚本的不变量。
- 升级兼容 Astro 6 的 Expressive Code 集成，构建不再输出 Markdown 旧插件接口弃用警告；Worker 状态语义代码和测试已修正，但本轮仍不部署 Worker、不启用云发布。

## 2026-09-02｜整站性能架构与资源生命周期优化

- 保留主页三层波浪、壁纸抽屉、主题圆形切换、磨砂和全部交互动效；本轮没有通过删除动画、降低壁纸分辨率或减少 Plot3D 点数换取性能。
- 壁纸控制器将几何测量从输入热路径前移并缓存，合并窗口与侧栏尺寸变化；收回后释放隐藏的高清位图，快速再次展开仍从浏览器缓存恢复。
- GitHub 语言、贡献记录和公共状态共用全局优先级请求池，最多同时执行三项公开数据请求；语言与贡献结果增加版本化跨会话定时缓存，降低移动网络请求风暴。
- Plot3D 改为接近视口后才加载 Plotly 并绘图，离开文章页时主动释放 WebGL；MiniBrowser 的 iframe 同样改为原生延迟加载，组件效果与交互保持不变。
- 修复 Astro 页面切换后 TOC 保留旧标题与滚动监听的问题；标题几何改为仅在正文布局变化时更新，滚动帧只使用缓存数值。
- 首页视口外信息区、维护条目和页脚增加浏览器渲染隔离，减少首屏样式、布局与绘制工作；本轮未修改或部署 Worker。
- 修复主页正文首个区块的外边距穿透抽屉容器、导致波浪下方露出水平背景带的问题；三层波浪的路径、配色与动画节奏保持不变。

## 2026-08-26｜全局壁纸手势与连续抽屉动效

- 桌面主页在页面顶部支持从任意鼠标位置向上滚动连续展开壁纸，全图状态从任意位置向下滚动收回；页面尚未到顶部时继续使用原生滚动，不干扰文章浏览。
- 移动端改为顶栏以下全局下拉展开、全图上推收回；收起状态的上滑仍用于正常浏览主页内容，多点与横向手势不会误触壁纸。
- 壁纸使用同一张已解码主题图片，通过 `clip-path` 与图片 `transform` 从封面取景连续过渡到完整取景，不再做封面／全图透明度交叉；高清资源仅在稳定展开后无闪烁替换。
- 三层波浪移到主页内容抽屉上沿，按真实舞台与封面高度和近期文章、状态及维护区域同步滑出和拉回；取消固定 96px 位移及内容淡出，顶栏和左侧栏始终固定。
- 手势结算统一使用可反向的 WAAPI 时间线与抽屉缓动，波浪在拖动、结算、展开、离屏、后台及降低动态模式下暂停；本轮未修改或部署 Worker。

## 2026-08-26｜恢复三层波浪视觉并保留无损性能优化

- 撤销把三层波浪拆成三个独立 SVG 的实验，恢复为单个 SVG 中复用一条原创路径的三个 `<use>` 图层；路径、层距、配色、透明度与 12／7／4 秒错速节奏均恢复到原有效果。
- 对照 astro-koharu 的公开实现后确认，轻量方案的关键是共享 SVG 路径并仅动画 `transform`，不是削减层数或视觉；本站继续使用自己的路径和配色，不复制其 AGPL-3.0 源码。
- 恢复移动端顶栏与壁纸箭头的磨砂效果；继续保留首图主题资源提前选择、响应式图片、异步解码、状态模块近视口加载，以及波浪在离屏、展开、后台和降低动态时暂停等不改变外观的优化。
- 增加视觉契约，固定单 SVG、三层 `<use>`、共享路径和磨砂降级边界；本轮未修改或部署 Worker。

## 2026-08-25｜移动加载可靠性与动画合成优化

- 主页首图在 HTML 解析阶段立即选择当前主题的响应式资源，不再等待壁纸交互模块初始化；浅色首图改用实测更小的 WebP，深色继续使用更省流量的 AVIF。
- 三层波浪保留原有路径、配色和错速节奏，但每层改为独立 SVG 合成层，避免移动浏览器反复重绘整块共享 SVG。
- 桌面端继续保留顶栏磨砂效果；移动端使用高不透明度主题背景替代实时背景模糊，减少滚动时的背景重采样。
- 运行状态完整逻辑改为接近视口或点击刷新时才下载，并修复 Astro 页面生命周期提前断开懒加载观察器的问题。
- 新增首图启动、波浪合成、移动模糊降级与状态按需加载契约测试；本轮未修改或部署 Worker。

## 2026-08-24｜主页手势壁纸与性能优化

- 白色箭头移到壁纸顶部中央；点击或在顶部手柄区上滑可展开完整壁纸，下滑、再次点击或按 Escape 可收回，顶栏始终固定且可操作。
- 桌面端补充鼠标滚轮控制：指针位于壁纸上方时向上滚动展开，全图状态向下滚动收回；收起状态向下滚动仍用于浏览页面，不劫持普通滚动。
- 展开层继续使用站长提供的 3840×2160 原图来源，并增加 2560/3840 高质量响应式资源；修复全局图片高度规则造成的底部裁切，展开前后统一综合色调。
- 桌面展开时常驻左栏不再收回，仅隐藏下方主页内容；底部波浪参考公开主题的轻量 SVG 运动机制，改为共用一条原创路径的三层错速动画，并继续使用本站红金／黑金配色。
- 展开进度直接跟随触摸或触控笔，壁纸与下方内容仅使用 `transform` 与 `opacity` 同步运动，常驻左栏保持不动；快速反向操作会从当前位置结算，不再生成全屏 View Transition 快照。
- 首屏改用构建期 AVIF/WebP 和 64px 低清底图，最高 3840px 的高清候选图仅在首次展开意图时加载；移除实时模糊、滚动缩放和无效合成层，波浪在展开、离屏、页面后台及降低动态模式下暂停。
- GitHub 语言、贡献记录和运行状态改为进入视口或用户展开时加载，搜索逻辑延迟到首次打开；外部中文字体改为系统字体栈，文章、状态和侧栏样式按对应页面加载。
- 本次只更新博客前端并发布 GitHub Pages；没有修改云发布开关，也没有部署 Worker。

## 2026-08-24｜主页主视觉照片更新

- 使用站长提供的 3840×2160 横向照片替换主页封面，并继续通过 Astro 输出响应式 WebP，避免移动端下载不必要的大图。
- 桌面端适度增加封面高度并校准人物焦点；移动端将文案留在左侧暗部、主体放在右侧，尽量保留人物、长椅与周边环境。
- 保留降低动态分支；后续性能优化已移除壁纸滚动缩放，避免常驻合成开销。

## 2026-08-24｜Worker 状态服务接入

- 部署 `blog-test2-admin-api` Cloudflare Worker，并为 `blog_test2` 创建独立 KV 命名空间；主页和管理台开始使用真实 Worker 健康检查与公共仓库状态。
- Pages 构建通过 `PUBLIC_ADMIN_SYNC_API_URL` 注入 Worker 地址，Worker 已验证可读取 `ice11123/blog_test2/main` 与最近一次 Pages 部署。
- GitHub OAuth App 的 Client ID/Secret 尚未配置，因此远程发布继续关闭；本次没有把状态服务正常误写成云端写入已启用。

## 2026-08-22｜公共页面常驻三态侧栏

- 公共桌面页面改用同一条常驻左侧栏，移除首页浮动资料卡、文章树与右侧目录相互割裂的旧布局。
- 文章页可在“站点概览 / 文章目录 / 系列文章”之间自由切换，支持方向键、Home、End 和完整 ARIA 状态；目录滚动高亮与标题锚点保持不变。
- 系列文章依据现有最深分类自动归组并高亮当前文章，不新增难以维护的伪系列字段；窄屏继续优先显示正文。
- 本次借鉴公开参考站的固定壳体和内容槽信息架构，使用本站 Astro 与原生 TypeScript 独立实现，未复制其 AGPL-3.0 源码，也未修改或部署 Worker。

## 2026-08-22｜首页运行状态误报与刷新入口修复

- 修复主页刷新按钮被标题区域样式一并隐藏的问题，按钮现位于“运行状态”标题右侧，并在检测期间显示旋转和“检测中”反馈。
- GitHub API 限流、代理超时、网络不可达与上游故障改为“等待确认”，不再误报为仓库或 Pages 异常；仓库、分支、工作流配置错误和真实部署失败仍显示异常。
- 手动刷新增加请求互斥，连续点击不会发起并发检测；Worker 仍保持未部署状态。

## 2026-08-22｜主页双栏与主题切换视觉修复

- 主页桌面端新增固定个人资料、站点统计和导航树，右侧内容直接进入近期文章；窄屏继续使用单栏，文章详情侧栏保持不变。
- 顶部磨砂背景改为全宽铺设，滚动时不再出现左右断边；导航内容仍保持居中宽度。
- 浅色／深色主题改为太阳、月亮按钮直接切换，指针点击支持以按钮为圆心的双向圆形过渡，键盘和降低动态模式立即切换。
- 无封面文章移除粉色渐变和巨大单字，改为主题自适应的中性文档预览；本轮未修改或部署 Worker。

## 2026-08-21｜移动布局与交互动效修复

- 非文章公共页面移除重复的完整左栏，文章页继续保留文章树和覆盖式目录，移动正文不再被三栏容器或宽内容裁切。
- 搜索、主题菜单与左右抽屉统一为短时、可反向的 transform/opacity 反馈；移除反复滚动显现、文章慢入场和树节点高度动画。
- TOC 指示器只更新 transform，首页图片缩放幅度收敛；降低动态模式停止波浪、缩放和抽屉位移。
- Spoiler 支持点击、Enter 和 Space 揭示；跨页点击站名立即返回首页，五连击管理入口仅在首页计数。
- 新增视觉契约测试，阻止重新引入 `transition: all`、关键布局属性动画和缺失的降低动态处理；Worker 与云发布配置未在本轮更改或部署。

## 2026-08-21｜本地发布、草稿与页面可靠性修复

- Worker 休眠代码的仓库目标和管理台回跳路径统一为 `blog_test2`，错误目标配置会拒绝写入；云发布仍保持关闭，本轮未执行 Worker 部署。
- 本地文章发布增加暂存区、分支、远端和同名目标检查，同名文章必须显式确认覆盖。
- 管理台改为只保存实际本地修改，自动迁移旧草稿；正式文章在云发布关闭时只能放弃本地修改，不再显示误导性删除操作。
- 修复文章标题深链接、首页 GitHub 数据重复请求和 Plotly 加载失败后持续轮询的问题。
- 根测试门禁覆盖本地状态、标题锚点、文章发布和 Worker，并在 Pages 构建前自动执行。

## 2026-08-15｜顶部图片主视觉与滚动放大

- 使用站长提供的横向图片替换首页旧渐变装饰背景，并通过 Astro 自动生成响应式 WebP。
- 默认仅展示经过裁切的封面区域；页面滚动时图片平滑放大，返回顶部自动恢复，减少动态偏好下保持静态。
- 分别校准桌面与移动端人物焦点，保留底部动态波浪和红金／黑金主题过渡。
- 左侧栏头像下方文案更新为“失败并不可怕，放弃才是可耻的。”。

## 2026-08-15｜波纹动态、常驻左栏与红金主题

- 波浪改为两条重复 SVG 轨道横向循环，解决动画运行但肉眼近似不动的问题。
- 首页和文章页统一复用左侧目录栏，桌面端常驻，文章导航与当前文章均显示选中状态。
- 浅色主题调整为中国红＋金色，深色主题调整为黑色＋金色，并同步头像、封面和目录高亮。

## 2026-08-15｜首页波纹主视觉与头像更新

- 参考公开开源站点的布局语言，重做首页顶部封面和动态波纹层。
- 移除旧首页的“离”字大球装饰，新增作者信息卡和快捷导航。
- 接入站长提供的猫咪头像，并修复移动端封面眉题裁切。

## 2026-08-15｜V2 内容优先首页与轻量顶栏

- 首页采用内容优先结构，并接入与文章页相同的左侧目录栏；文章页继续保留左右目录侧栏。
- 首屏重构为作者介绍、核心统计与近期文章，语言、状态、公告和 GitHub 贡献降级到页尾信息带。
- 公共顶栏移除折叠箭头，改为站名、主导航、搜索、浅色/深色主题与社交入口；隐藏管理入口继续保留。
- 主题从六套迁移为 `light` / `dark` 两套，旧浏览器主题值会自动转换。
- 修复首屏图文列顺序、移动端搜索图标被隐藏和主题选择后菜单不收起的问题，并完成 1440×900、390×844 实际截图复核。

## 2026-08-15｜blog_test2 独立视觉实验版

- 从 `blog_test1` 快照创建独立仓库 `ice11123/blog_test2`，保留完整 Git 历史并切换为 `/blog_test2/` 子路径。
- 首页先按新的视觉规范重做；管理台继续支持本地草稿与导出，但云端发布暂时禁用，避免误写旧仓库。
- 新增 `DESIGN.md`，固定颜色角色、字号层级、4/8px 间距、圆角、阴影边界和异步状态规范。
- 修复首次部署遗漏 Astro base 配置导致的旧子路径资源引用，线上资源和文章链接统一切换到 `/blog_test2/`。
- 完成线上 `1440×900` 与 `390×844` 浏览器复核，确认搜索、主题切换、文章链接和管理台禁写状态正常；验收截图归档在 `artifacts/`。

## 2026-08-15｜本地仓库目录统一

- 将本地博客仓库统一迁移到 `D:\A Study files\AI_files\blog\blog_test1`，保留 Git 历史、`main` 分支和 GitHub 远程配置。
- 在新目录重新核对依赖和项目文件，Astro 检查结果为 0 错误、0 警告。

## 2026-08-15｜迭代日志与验收记录补全

- 对照 Git 提交重新整理首页公共状态、仪表盘布局、部署验收和截图归档记录。
- 将近期记录提交与 `progress.md`、`findings.md`、Beads 状态逐一关联，后续每轮维护会持续同步计划、排查结论、进度和提交结果。

## 2026-08-14｜语言构成、公告与贡献墙视觉优化

- 将“本网站仓库语言构成”和“我的 GitHub 语言构成”合并为一个“代码语言构成”面板，桌面端并排、窄屏自动堆叠。
- 增大近期维护公告和系统状态的可用空间，状态卡片改为更易读的尺寸与间距。
- 重构 GitHub 贡献墙标题、说明、网格容器和居中方式，保留移动端内部横向滚动。
- 已通过 `pnpm run check`、`pnpm run build`、Worker 测试、Node 语法检查和 `git diff --check`。

## 2026-08-14｜首页统一仪表盘布局重构

- 移除首页月度发文统计，将近期文章调整到首行并限制为最新 3 篇。
- 欢迎语与六张统计卡统一居中，系统状态、语言构成和维护公告纳入同一响应式网格。
- GitHub 贡献墙移动到首页最底部并横跨主体；宽屏三列、中屏两列、移动端单列均完成复核。
- 修复移动端首次加载时右侧目录栏短暂遮挡正文的问题。
- 修复完整刷新时滚动渐入脚本未初始化导致统计卡保持透明的问题。
- 首页统计卡增加首屏可见性兜底，不依赖客户端观察器才能显示核心数据。
- 提交 `11d6850` 已部署到 GitHub Pages，线上首页复核通过。

## 2026-08-14｜首页状态与公告栏并行布局优化

- 系统状态卡片改为桌面端两列两行，管理台前端与 Cloudflare Worker、目标仓库与 GitHub Pages 分行展示。
- 状态栏与维护公告栏统一外框宽度和左右边界，宽屏信息栏适度增宽，同时保留左右目录侧栏与移动端单列布局。
- 通过实际浏览器断点复核 1680、1280 和 390 像素视口，确认无横向溢出。

## 2026-08-14｜首页系统状态刷新提速

- 首页状态超过 900ms 未返回时提前启用 GitHub 公共 API，不再等待多轮 Worker 超时。
- 旧缓存只作为临时信息展示，并继续刷新最新仓库提交和 Pages 部署。
- Worker 健康检查与仓库检查独立更新，快速结果不会再被慢请求拖住。

## 2026-08-14｜公共状态自检误报修复

- 区分 Worker 服务错误与用户网络/DNS 不可达，网络异常不再直接显示为红色故障。
- Worker 自检增加自动重试，并将 `/health` 与仓库部署状态分开检测。
- Worker 暂时不可直连时，仓库和 GitHub Pages 继续通过 GitHub 公共 API 降级显示。

## 2026-08-13｜管理台预览与布局稳定性升级

- 修复 KaTeX 公式定位、代码高亮和 Spoiler 折叠内容显示。
- 实时预览改为固定高度、内部滚动，长文章不再无限拉长页面。
- 正文编辑框增高，并支持连续空行的可视化预览。
- 首页恢复左右侧栏，重新校准字号、卡片密度和双列对齐。

## 2026-08-13｜文章发布安全与一致性修复

- 使用 HttpOnly Cookie、严格 Origin 和 CSRF 校验保护写入接口。
- 文章创建、更新、移动和删除改为一次 Git 引用更新，避免新旧版本并存。
- 修复正式文章路径扩展名、路径冲突和旧草稿兼容问题。
- 管理台增加 Worker、仓库和 GitHub Pages 状态检测。

## 2026-08-12｜云端文章发布链路上线

- 接入 GitHub OAuth 和 Cloudflare Worker，可从管理台提交文章到仓库。
- GitHub Actions 自动构建并发布到 GitHub Pages。
- 支持本地草稿、文章导出、远程更新和删除。

## 2026-08-12｜管理台编辑器原型上线

- 增加隐藏管理入口、文章目录树、标签和目录编辑功能。
- 提供 Markdown / MDX 正文编辑、实时预览和浏览器本地草稿。
- 支持 Mermaid、KaTeX、代码块以及常用 MDX 组件的安全预览。

## 2026-08-11｜博客源码迁移与站点初始化

- 完成授权源码迁移，清理原作者文章、账号和个人素材。
- 站点身份更新为“离子怪的博客”，适配 `/blog_test1/` 子路径。
- 配置 GitHub Pages、移动端顶栏、六套主题和左右侧栏。
